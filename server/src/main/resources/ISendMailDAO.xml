<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="server.dao.ISendMailDAO">
    <resultMap id="mailEntity" type="entity.db.DbSendMailEntity">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="targetUserId" column="target_user_id"/>
        <result property="srcUserId" column="src_user_id"/>
        <result property="srcUserName" column="src_user_name"/>
        <result property="propsId" column="props_id"/>
        <result property="propsNumber" column="props_number"/>
        <result property="money" column="money"/>
        <result property="date" column="date"/>
        <result property="state" column="state"/>
    </resultMap>

    <insert id="insertMail" useGeneratedKeys="true" keyProperty="id" keyColumn="id" parameterType="entity.db.UserEntity">
        INSERT INTO `send_mail` ( `title`,`target_user_id`, `src_user_id`,`props_id`,`props_number`,`money`,`date`,`state`,`src_user_name` )
        VALUES ( #{title},#{targetUserId}, #{srcUserId},#{propsId} ,#{propsNumber},#{money},#{date},#{state},#{srcUserName});
    </insert>

    <select id="selectMailWithinTenDay" resultMap="mailEntity" resultType="list">
        SELECT * FROM `send_mail` WHERE target_user_id=#{targetUserId} AND `date` > #{date} AND state = 0;
    </select>

    <update id="updateMailBatch" parameterType="collection">
        <foreach collection="mailCollection" item="mail" index="index" open="" close="" separator="">
            update `send_mail`
            <set>
                <if test="mail.state != 0">
                    state = #{mail.state}
                </if>

            </set>
            where id = #{mail.id};
        </foreach>
    </update>

</mapper>