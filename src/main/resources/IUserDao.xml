<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="server.dao.UserDAO">
    <resultMap id="userEntity" type="server.entity.UserEntity">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="password" column="password"/>
    </resultMap>

    <select id="getUserByName" resultMap="userEntity">
        SELECT id, user_name, `password`, salt FROM `user` WHERE user_name = #{userName};
    </select>

    <insert id="insertInto" useGeneratedKeys="true" keyProperty="id" keyColumn="id" parameterType="server.entity.UserEntity">
        INSERT INTO `user` ( user_name, `password` ) VALUES ( #{userName}, #{password} );
    </insert>
</mapper>
